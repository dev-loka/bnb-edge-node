import { HardhatUserConfig } from "hardhat/config";
import "@nomicfoundation/hardhat-toolbox";
import * as dotenv from "dotenv";

dotenv.config();

const config: HardhatUserConfig = {
    solidity: {
        compilers: [
            {
                version: "0.8.25",
                settings: {
                    evmVersion: "cancun"
                }
            }
        ]
    },
    networks: {
        hardhat: {
            chainId: 1337,
        },
        opbnbTestnet: {
            url: process.env.OPBNB_RPC,
            chainId: 5611,
            accounts: process.env.PRIVATE_KEY ? [process.env.PRIVATE_KEY] : [],
        },
        opbnbMainnet: {
            url: "https://opbnb-mainnet-rpc.bnbchain.org",
            chainId: 204,
            accounts: process.env.PRIVATE_KEY ? [process.env.PRIVATE_KEY] : [],
        },
    },
    etherscan: {
        apiKey: {
            opbnbTestnet: process.env.OPBNBSCAN_API_KEY as string,
            opbnbMainnet: process.env.OPBNBSCAN_API_KEY as string,
        },
    },
    mocha: {
        timeout: 100000000,
    },
};

export default config;

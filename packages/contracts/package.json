{
    "name": "@bnb-edge/contracts",
    "version": "1.0.0",
    "scripts": {
        "build": "hardhat compile",
        "test": "hardhat test",
        "deploy": "hardhat run scripts/deploy.ts --network opbnbTestnet",
        "lint": "solhint 'contracts/**/*.sol'"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.2"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "hardhat": "^2.22.6",
        "solhint": "^5.0.1",
        "dotenv": "^16.4.5"
    }
}
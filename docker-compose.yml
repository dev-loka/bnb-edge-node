services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: bnb_edge
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  web:
    build: ./apps/web
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - postgres

  ai:
    build: ./apps/ai-service
    ports:
      - "8000:8000"
    env_file:
      - .env

volumes:
  pgdata:
